# Notebooks

Use this directory for exploratory analysis or classroom demonstrations. The canonical, reproducible pipeline lives in `scripts/run_analysis.py`; notebooks should import the same helpers from `src/fractal_dimension` and save artifacts into `results/` with deterministic filenames.
